services:
  - type: web
    name: erp-backend
    env: node
    buildCommand: npm install --include=dev
    startCommand: npm run build && node dist/main
    envVars:
      - key: NODE_ENV
        value: production
